using UnityEngine;
using System.Collections;

public class Player : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//this.gameObject.transform.position = GameObject.Find("Begin");
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
